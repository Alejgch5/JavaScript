--Declara variables de JavaScript
JAVASCRIPT TIPO DE DATOS
almacenables en variables
-undefined
-null
-boolean
-string
-symbol
-bigint
-number
-object

--Almacenar valores con el operador de asignación
var myName;
myName=7; asignacion de variable

--Asigna el valor de una variable a otra variable
var myName;
myName=7;
var myNum;
myNum=myName; asignacion de la primera variable

--Inicializa variables con el operador de asignación
var myName = 9; inicializar variable con valor inicial 

--Declara variables de cadena
var myFirstName = "Alejandro"; cadena literal serie de ceros
var myLastName = "Garcia";

--Comprendiendo las variables no inicializadas
// Cambia solo el código debajo de esta línea
var a = 5;
var b = 10;
var c= "I am a";
// Cambia solo el código encima de esta línea

a = a + 1;
b = b + 5;
c = c + " String!";

--Comprendiendo la sensibilidad de mayúsculas en las variables
BUENAS PRACTICAS= camelClase

var someVariable;
var anotherVariableName;
var thisVariableNameIsSoLong;

--Explora las diferencias entre las palabras claves var y let
se usa cuando se sobreescribe una variable 

let catName = "Oliver";
let catSound = "Meow!";

--Declara una variable de solo lectura con la palabra clave const
variables constantes

const FCC = "freeCodeCamp"; // Cambia esta línea
let fact = "is cool!"; // Cambia esta línea
fact = "is awesome!";
console.log(FCC, fact)

--Suma dos números con JavaScript
operaciones
const sum = 10 + 10;
const sum = 10 - 10;
const sum = 10 * 10;
const sum = 10 / 10;

--Incrementa un número con JavaScript
let myVar = 87;
myVar++;
decrementar
myVar--;

